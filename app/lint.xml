<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!-- Mirrors the -opt-in compiler flag in app/build.gradle.kts. That flag
         only satisfies the Kotlin compiler's opt-in requirement; Android
         Lint runs its own separate UnsafeOptInUsageError check against
         RequiresOptIn-annotated APIs (SegmentedButton, DatePicker, ...) and
         needs to be told about the same intentional opt-in here. -->
    <issue id="UnsafeOptInUsageError">
        <option name="opt-in" value="androidx.compose.material3.ExperimentalMaterial3Api" />
    </issue>
</lint>
