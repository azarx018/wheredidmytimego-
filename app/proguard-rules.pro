# Room entities/DAOs are annotation-processed at compile time; no extra rules
# typically required with KSP. Add app-specific keep rules here only if a
# release build crashes due to reflection-based access.
